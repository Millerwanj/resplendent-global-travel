# Deployment
This is a static HTML/CSS/JS repository suitable for GitHub Pages, Netlify, Cloudflare Pages or conventional hosting.
Set `index.html` as the entry file. Confirm the final domain before updating sitemap and metadata.
