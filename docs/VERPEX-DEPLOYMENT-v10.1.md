# Verpex Deployment — v10.1.0 Across the Virungas Edition

Deploy only after the local desktop and mobile review is accepted and
Volcanoes Safaris has approved the completed feature in writing.

1. Back up the current `public_html` production files.
2. Upload
   `Resplendent-Global-Travel-v10.1-Across-the-Virungas-Gorilla-Collection-Review-Candidate.zip`.
3. Extract into `public_html`, preserving `.htaccess` and directory structure.
4. Confirm the homepage, `/signature-journeys`,
   `/signature-journey-uganda-rwanda-gorillas`, `/journal` and
   `/journal-uganda-rwanda-gorillas`.
5. Confirm all eight Rwanda–Uganda images load, use distinct files and display
   without source captions.
6. Open a `Tailor This Journey` link and verify the service, journey name,
   destination and opening message are prefilled.
7. Re-submit the two new clean URLs in Google Search Console after deployment.

Do not publish this supplier-review candidate until Volcanoes Safaris has
approved the completed feature in writing.
