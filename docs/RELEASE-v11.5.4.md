# Resplendent Global Travel Solutions v11.5.4

## Release scope

- Adds a premium widescreen Global Business Connections film to `business.html`.
- Preserves the complete delegation within the frame on desktop and mobile.
- Uses click-to-play, `preload="none"` and a lightweight web encode.
- Adds discreet Instagram, Facebook and LinkedIn links to `contact.html`.
- Includes a separate 1080 × 1920 Meta-ready B2B Reel with a branded end card.

## Deployment

Upload the contents of the Verpex ZIP directly into `public_html`, preserving the directory structure. Clear the browser cache after deployment so `styles.css?v=11.5.4` is loaded.

## Verification

- Open `/business` and confirm the poster shows the full delegation.
- Select Play and confirm the film loads without autoplay.
- Open `/contact` and verify the three social links.
- Test at desktop and mobile widths.
