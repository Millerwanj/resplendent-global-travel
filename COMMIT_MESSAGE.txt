Suggested commit message for this push:

    v4.3.2 — production-grade rebuild on the v4.2 skeleton

    - Full editorial design system (Cormorant Garamond + Jost, navy/gold
      brand tokens, fluid type + spacing scale)
    - All homepage sections built out in full: hero, About Resplendent,
      Featured Destinations, Corporate Travel, Global Business
      Connections, Start Your Journey, Contact, footer
    - Vanilla JS: header scroll state, mobile nav, scroll reveal,
      contact form validation + zero-backend mailto submission
    - Accessibility: skip link, focus states, labelled form fields,
      aria-live status, prefers-reduced-motion support throughout
    - SEO: meta description/OG/Twitter tags, JSON-LD, sitemap, robots.txt
    - Real favicon.ico / apple-touch-icon.png / og-cover.jpg generated
      from the brand monogram
    - Added privacy.html and terms.html (previously dead-linked to 404)
    - Full link/asset/accessibility validation pass — see CHANGELOG.md

If you're using git from the command line, something like:

    git add .
    git commit -F COMMIT_MESSAGE.txt
    git push

Feel free to delete this file once you've made the commit — it's a
one-time note, not part of the shipped site.
